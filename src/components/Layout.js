import React from 'react';

export default ({ children }) => (
  <main
    style={{
      margin: '0 auto',
      maxWidth: '47rem',
      padding: '0 1rem 4rem 1rem',
    }}>
    {children}
  </main>
);
